(function() {
  $("#topic_entry_<%= @topic_entry.id %>").replaceWith('<%= escape_javascript(render "topic_entry", :topic_entry => @topic_entry, :topic_entry_counter => @topic.topic_entries.count - 1) %>')
})();
