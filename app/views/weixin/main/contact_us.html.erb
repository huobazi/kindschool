<div class="container">
  <div class="bind">
    <div class="logo">
      <img src="<%= @kind.asset_img ? @kind.asset_img.public_filename : '/t/colorful/logo.png' %>" width="100" height="79" alt="Logo"/>
    </div>
    <div class="about">
      <% if(about_me = @kind.page_contents.find_by_number("contact_us")) && (entry = about_me.content_entries.first) %>
        <%= raw entry.content %>
      <% else %>
        还未录入信息。
      <% end %>
    </div>
  </div>
</div>